page, .root {
  --gap: 30rpx;
  --color-money: #ff5000;

  line-height: 1;
}

page {
  padding: 0 var(--gap);
}
.fullwidth {
  margin-left: calc(50% - 50vw);
}
page, view {
  box-sizing: border-box;
}

input {
  line-height: 1;
  font-size: inherit;
  height: 1em;
}