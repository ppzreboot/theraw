@import "/global.wxss";

swiper, swiper image {
  width: 750rpx;
  height: 750rpx;
  display: block;
}

.basic {
  padding: var(--gap) 0;
}
.basic .price {
  font-size: large;
  font-weight: 500;
  color: var(--color-money);
}
.basic .title {
  font-weight: 500;
  line-height: 3;
}
